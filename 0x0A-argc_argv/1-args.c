#include <stdio.h>

/**
  * main - print number followed by new line
  * @argv: the number to print
  * @argc: the number
  * Return: 0
  */

int main(int argc, char *arg[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
