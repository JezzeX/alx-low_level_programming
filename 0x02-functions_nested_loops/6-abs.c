#include "main.h"

/**
  * _abs - prints absolute value of integer
  * @n: the integer to check
  * Return: absolute value or negative value
  */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
